{
    "name": "@root/default",
    "private": true,
    "scripts": {
        "commit": "npx openrouter-commit -run",
        "commit2": "git add . && git commit -m 'fixes' && git push",
        "build": "mkdocs build",
        "serve": "mkdocs serve"
    },
    "devDependencies": {
        "openrouter-commit": "latest"
    }
}
